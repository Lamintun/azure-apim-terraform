openapi: 3.0.1
info:
  title: test2
  description: Import from "lamin-function-test" Function App
  version: '1.0'
servers:
  - url: https://function-lamin-test.azurewebsites.net/api/
paths:
  /HttpTrigger1:
    get:
      summary: test2
      operationId: get-test2
      responses:
        '200':
          description: 
    post:
      summary: test2
      operationId: post-test2
      responses:
        '200':
          description: 
components:
  securitySchemes:
    apiKeyHeader:
      type: apiKey
      name: Ocp-Apim-Subscription-Key
      in: header
    apiKeyQuery:
      type: apiKey
      name: subscription-key
      in: query
security:
  - apiKeyHeader: [ ]
  - apiKeyQuery: [ ]
